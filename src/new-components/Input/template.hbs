<input
  class="mfm-input mfm-typography__text_m {{#if invalid}}mfm-input_invalid{{/if}}"
  placeholder="{{placeholder}}"
  {{#if name}}
  name="{{name}}"
  {{/if}}
  {{#if disabled}}
  disabled
  {{/if}}
  type="{{type}}"
>
