export type ChangeUserPasswordParams = {
  oldPassword: string;
  newPassword: string;
};

export type ChangeUserInfoParams = {
  firstName: string;
  secondName: string;
  displayName: string;
  login: string;
  email: string;
  phone: string;
};
