.mfm-loading-overlay {
  z-index: 2;
  position: fixed;
  top: 0;
  bottom: 0;
  left: 0;
  right: 0;
  background: rgb(255 255 255 / 30%);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--mfm-color-blue);

  & > svg {
    width: 60px;
    height: 60px;
  }
}
