.mfm-avatar {
  flex-shrink: 0;
  width: 50px;
  height: 50px;
  border-radius: 50%;
  overflow: hidden;
  border: 1px solid gray;
  box-sizing: border-box;
  background-color: var(--mfm-color-gray-light);
  background-size: cover;
  background-position: center;
}
